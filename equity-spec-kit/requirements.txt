PyYAML==6.0.3
# warehouse machine only (the Parquet codec); validated by tests/test_m2.py --require-parquet
pyarrow==25.0.1
# Runtime timezone database required by PyArrow on clean Windows installs.
tzdata==2026.4
